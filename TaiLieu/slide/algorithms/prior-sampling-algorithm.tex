\code{
\func{PriorSample}{\v{bn}}{an event sampled from $\pv(X_1,\ldots,X_n)$ specified by \v{bn}}
    \setq{\mbf{x}}{an event with $n$ elements}
    \k{for} $i = 1$ \k{to} $n$ \k{do}
          \setq{$x_i$}{a random sample from $\pv(X_i | Parents(X_i))$}
    \k{return} \mbf{x}
}