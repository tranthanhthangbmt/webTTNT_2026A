\code{
\func{Iterative-Deepening-Search}{{\ts}\var{problem}}{a solution}
      \firstinputs{problem}{a problem}
\bodysep
      \key{for} \setq{\var{depth}} 0 \key{to} $\infty$ \key{do}
          \setq{\var{result}}{\prog{Depth-Limited-Search}({\ts}\var{problem{\ac}depth})}
          \key{if} \var{result} $\neq$ cutoff \key{then} \key{return} \var{result}
      \key{end}
}


