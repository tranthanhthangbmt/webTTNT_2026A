\code{
\key{datatype} \prog{CSP-State}
    \key{components}: \prog{Unassigned}, danh sách các biến chưa được gán
    \phantom{\key{components}: }\prog{Assigned}, danh sách các biến đã có giá trị

\key{datatype} \prog{CSP-Var}
    \key{components}: \prog{Name}, phục vụ cho mục đích i/o
    \phantom{\key{components}: }\prog{Domain}, danh sách các giá trị có thể
    \phantom{\key{components}: }\prog{Value}, giá trị hiện tại (nếu có)


}
