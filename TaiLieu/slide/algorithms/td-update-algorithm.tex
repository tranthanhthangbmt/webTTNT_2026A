\code{
\func{TD-Update}{\var{U{\ac}e{\ac}percepts{\ac}M{\ac}N}}{the utility table \var{U}}
\bodysep
    \key{if} \prog{Terminal?}[\var{e}] \key{then}
          \setq{\var{U}[\noprog{State}[\var{e}]]}{\prog{Running-Average}(\var{U}[\noprog{State}[\var{e}]]\var{{\ac}}\noprog{Reward}[\var{e}]\var{{\ac}N}[\noprog{State}[\var{e}]])}
    \key{else if} \var{percepts} contains more than one element \key{then}
          \setq{\var{e}$'$}{the penultimate element of \var{percepts}}
          \setq{\var{i}{\ac}\ \var{j}}{\noprog{State}[\var{e}$'$]{\ac}\ \noprog{State}[\var{e}]}
          \setq{\var{U}[i]}{\var{U}[i] + $\alpha({\var{N}[\var{i}]})$(\noprog{Reward}[\var{e}$'$] + \var{U}[\var{j}] - \var{U}[\var{i}])}
}
