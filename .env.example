# Đổi tên file này thành .env (bỏ .example đi)
# Sau đó điền API Key thực sự của bạn vào.
# File .env đã được cấu hình trong .gitignore nên sẽ không bao giờ bị lộ lên GitHub.

GEMINI_API_KEY=điền_api_key_của_bạn_vào_đây
